import { SketchUnderline, MarkerHighlight, ChalkDoodle, StackTable } from "@/components/sketch";

export const metadata = {
  title: "TherapyVault — secure therapie-video",
  description:
    "Beveiligd video-platform voor therapeuten en cliënten. Granulaire access-grants, MFA, GDPR-export en payment-abstractie. Next.js 15 + Prisma + Cloudflare R2.",
  // Dutch-only page; no translated version exists, so self + x-default only.
  alternates: {
    canonical: "/projects/therapyvault",
    languages: {
      "nl-NL": "/projects/therapyvault",
      "x-default": "/projects/therapyvault",
    },
  },
};

export const appSchema = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "@id": "https://nathanrenting.dev/projects/therapyvault#app",
  name: "TherapyVault",
  description:
    "Een beveiligd video-platform waar therapeuten opgenomen sessies veilig met hun eigen cliënten delen. Signed URLs, granulaire access-grants per cliënt, MFA, GDPR-export en scheduled-deletion. Gebouwd op Next.js 15, Prisma en Cloudflare R2.",
  applicationCategory: "HealthApplication",
  operatingSystem: "Web",
  inLanguage: "nl-NL",
  url: "https://nathanrenting.dev/projects/therapyvault",
  image: "https://nathanrenting.dev/therapyvault-architecture.png",
  author: { "@id": "https://nathanrenting.dev/#nathan" },
  creator: { "@id": "https://nathanrenting.dev/#nathan" },
  publisher: { "@id": "https://nathanrenting.dev/#org" },
  keywords: [
    "secure video platform",
    "therapy",
    "GDPR",
    "access control",
    "MFA",
    "Next.js",
  ],
};

<div className="max-w-3xl mx-auto px-6 py-16 md:py-20 relative">

<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{ __html: JSON.stringify(appSchema) }}
/>

<div className="mb-2 font-mono text-xs uppercase tracking-widest text-cyan-400">
  <SketchUnderline seed={223}>Project · v0.1 release candidate</SketchUnderline>
</div>

# TherapyVault — secure video-platform voor therapie

<MarkerHighlight>Een privé video-platform waar therapeuten opgenomen sessies veilig met hun eigen cliënten delen.</MarkerHighlight>{" "}
Geen public URLs, geen YouTube-link met &quot;alleen wie de link heeft&quot;
— alles achter signed URLs met granulaire access-grants per cliënt,
optioneel time-limited, view-limited of download-limited.

<figure className="my-10 -mx-2 md:-mx-8">
  <img
    src="/therapyvault-architecture.png"
    alt="Hand-getekende architectuur-schets: therapist uploadt naar VAULT (notebook met slot), MFA-shield voor admin, drie clients met access-grants (forever / time-limited / view-limited), audit-log notitieboek aan onderkant, annotaties 'no public URLs', 'access-grant evaluator', 'GDPR'."
    className="w-full rounded border border-zinc-800"
    loading="lazy"
  />
  <figcaption className="mt-3 text-center text-xs font-mono text-zinc-500 uppercase tracking-widest">
    Whiteboard-schets · de vorm van het systeem
  </figcaption>
</figure>

<ChalkDoodle type="x-mark" className="mt-12 mb-2 opacity-50" />

## Probleem

WeTransfer is niet GDPR-compliant voor zorggegevens. YouTube unlisted
is geen access-control. SharePoint is enterprise-overkill. Vimeo-Pro
+ Memberstack is fragiel en duur.

TherapyVault is de specifieke oplossing: één plek waar de therapeut
uploadt en de cliënt veilig kijkt, met de juridische en
technische fundering die zorgcontext vereist.

<ChalkDoodle type="checkmark" className="mt-12 mb-2 opacity-50" />

## Wat er v0.1 in zit

| Area | Status |
|---|---|
| **Auth** | Email+password, password reset, **MFA (TOTP)**, session management, RBAC |
| **Video upload** | Resumable multipart (tus protocol) tot 10 GB, naar R2 / S3 / MinIO |
| **Video streaming** | Signed-URL playback met range-support, geen public URLs |
| **Access control** | Per grant: forever / time-limited / view-limited / download-limited |
| **Coupons** | 5 types: free access, % off, fixed off, download credit, view credit |
| **Payments** | Stripe (one-time + subscriptions) + Mollie-stub achter `PaymentProvider`-interface |
| **Dashboards** | Therapist, Client, Admin |
| **GDPR** | Data-export ZIP + scheduled-deletion met grace-period |
| **i18n** | EN + NL vanaf dag één (`next-intl`) |
| **Audit log** | Elke gevoelige mutatie wordt vastgelegd |

<ChalkDoodle type="asterisk" className="mt-12 mb-2 opacity-50" />

## Stack

<StackTable rows={[
  { label: "Frontend + API", value: "Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui" },
  { label: "Auth", value: <>Auth.js v5 — credentials + <strong>Argon2id</strong> hashing + TOTP MFA</> },
  { label: "Database", value: "PostgreSQL 16 + Prisma 6" },
  { label: "Storage", value: "Cloudflare R2 (S3-compatibel) — provider-abstractie ondersteunt S3, B2, MinIO, local" },
  { label: "Uploads", value: <>tus protocol via <code className="font-mono text-cyan-300">@tus/server</code></> },
  { label: "Payments", value: <><code className="font-mono text-cyan-300">PaymentProvider</code>-interface met Stripe-live + Mollie-stub erachter</> },
  { label: "Email", value: "console-stub (v0.1) — react-email templates die naar logs renderen" },
  { label: "Rate-limit", value: "Redis token-bucket met in-memory fallback" },
  { label: "Logging", value: "pino structured JSON + aparte AuditLog-tabel" },
  { label: "Tests", value: "Vitest unit/integration + Playwright e2e" },
]} />

<ChalkDoodle type="star" className="mt-12 mb-2 opacity-50" />

## Security-primitives

**Geen public URLs.** Elke video-stream gaat door een signed-URL die
de server pas uitgeeft na een access-grant-check. Range-headers
worden ondersteund zonder dat de URL ooit lekt buiten de sessie.

**MFA verplicht voor admins.** TOTP-app koppelen is een eerste-login
requirement; geen toegang tot admin-dashboards zonder.

**Argon2id** als password-hash met memory-hard parameters.

**GDPR-by-design.** Cliënt-data-export als ZIP, account-deletie via
scheduled-job met grace-period, AuditLog-tabel voor alle gevoelige
mutaties.

**Payment-provider-abstractie.** Stripe live, Mollie achter dezelfde
interface — switchen voor iDEAL zonder de business-logica te raken.

<ChalkDoodle type="arrow-curve" className="mt-12 mb-2 opacity-50" />

## Roadmap

| Versie | Wat |
|---|---|
| v0.1 (nu) | Alle critical security-primitives, productie-deploybaar |
| v0.2 | UI-polish, multi-bitrate HLS transcoding, push-notifications, real email-provider |
| v0.3+ | Custom domain per therapeut, white-label, live-session |

<ChalkDoodle type="checkmark" className="mt-12 mb-2 opacity-50" />

## Status

v0.1 release candidate. SMTP-provider en real-email-templates zijn
de volgende stap voor launch. Klant en specifieke use-case onder NDA.

</div>
